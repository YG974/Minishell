1) leaks quand /bin/qsdqsd 
	aussi le retour est 126, sur bash cest 127

2) leaks quand echo $HOME ou cat $HOME
	bref quand $variable leaks.

3) Erreur sur echo "salut $HOME"
	la variable disparait de la string quand entre quotes
	echo "salut $HOME coucou" => "salut  coucou"

4) leaks quand unset var

5) leaks sur echo bonjour ;|, echo bonjour >, echo <, echo ;;
	sur les erreurs de syntax

6) ERREUR $var1$var2 Makefile, ou echo $var$HOME
	les deux variables sont zappées au lieu de join
	surement le même problème que 2) et/ou 3)




